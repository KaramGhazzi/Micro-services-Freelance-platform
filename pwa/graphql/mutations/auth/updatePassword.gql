mutation UpdatePassword(
  $email: String!
  $currentPassword: String!
  $password: String!
) {
  updatePassword(
    email: $email
    currentPassword: $currentPassword
    password: $password
  ) {
    success
  }
}
