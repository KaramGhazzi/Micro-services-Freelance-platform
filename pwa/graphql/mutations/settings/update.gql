mutation settingUpsert(
  $key: String!
  $value: String!
  $type: SettingType!
  $settingId: Int
) {
  settingUpsert(key: $key, value: $value, type: $type, settingId: $settingId) {
    id
    key
    value
    type
  }
}
